name: Danger Dash Test CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install build tools
      run: |
        sudo apt-get update
        xargs -a apt-packages.txt -r sudo apt-get install -y

    - name: Install dependencies
      run: make

    - name: Run test
      run: make test
